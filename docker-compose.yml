services:
  postgres:
    image: postgres:16-alpine
    container_name: ai-admin-assistant-db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: ai_admin_assistant
    ports:
      - '5432:5432'
    volumes:
      - .data/postgres:/var/lib/postgresql/data
